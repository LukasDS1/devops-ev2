version: '3.8'
services:
  frontend:
    image: dockermasterluka/despacho-frontend:${VERSION:-v1}
    container_name: frontend
    ports:
      - "80:3000"   
      - "3001:3001"  
    volumes:
      - ./db.json:/app/db.json  # reemplaza el volumen de nginx-logs
    restart: unless-stopped