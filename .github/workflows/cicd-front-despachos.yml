name: CI/CD - Despacho Frontend


on:
  push:
    branches: [deploy]
    paths:
      - 'front_despacho/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Obtener version
        id: version
        run: echo "VERSION=v$(date +%Y%m%d%H%M%S)" >> $GITHUB_OUTPUT

      - name: Login a Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build y Push imagen
        run: |
          docker build -t dockermasterluka/despacho-frontend:${{ steps.version.outputs.VERSION }} ./front_despacho
          docker push dockermasterluka/despacho-frontend:${{ steps.version.outputs.VERSION }}

      - name: Desplegar en EC2 via SSM
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_SESSION_TOKEN: ${{ secrets.AWS_SESSION_TOKEN }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
        run: |
          aws ssm send-command \
            --instance-ids ${{ secrets.EC2_FRONTEND_INSTANCE_ID }} \
            --document-name "AWS-RunShellScript" \
            --parameters '{"commands":[
              "docker pull dockermasterluka/despacho-frontend:${{ steps.version.outputs.VERSION }}",
              "docker stop frontend || true",
              "docker rm frontend || true",
              ""cd /home/ec2-user && VERSION=${{ steps.version.outputs.VERSION }} docker-compose -f docker-compose.frontend.yml up -d frontend"
            ]}'
